# Core dependencies for Chasing Your Tail (CYT)
requests>=2.28.0
cryptography>=40.0.0

# ── Signal monitoring (install what matches your hardware) ──────────────────

# TPMS / RTL-SDR — also requires the rtl-433 system binary:
#   sudo apt install rtl-433
# No Python package needed; rtl_433 is invoked as a subprocess.

# Bluetooth / BLE — requires bluez system package:
#   sudo apt install bluez
# BLE scanning also needs elevated privileges:
#   sudo setcap cap_net_raw+ep $(which python3)
# No Python package needed; hcitool is invoked as a subprocess.

# ANT+ (requires ANT+ USB dongle, e.g. Dynastream ANTUSB-m):
# openant>=1.3.0

# Meshtastic / LoRa (requires Meshtastic-compatible LoRa device via USB or TCP):
# meshtastic>=2.3.0
# pypubsub>=4.0.3

# ── Optional enhanced features ──────────────────────────────────────────────

# For web dashboard (future enhancement)
# flask>=2.3.0
# flask-socketio>=5.3.0

# For advanced data analysis
# pandas>=1.5.0
# numpy>=1.24.0
# matplotlib>=3.7.0

# For testing
# pytest>=7.0.0
# pytest-cov>=4.0.0